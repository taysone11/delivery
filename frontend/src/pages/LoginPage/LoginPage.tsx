import styles from './LoginPage.module.css';

export function LoginPage() {
  return (
    <section className={styles.container}>
      <h1 className={styles.title}>Вход</h1>
      <p className={styles.text}>Форма входа будет на следующем этапе.</p>
    </section>
  );
}
